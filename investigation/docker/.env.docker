# Docker Test Environment Configuration
# Used by run-tests.sh for integration testing

# PostgreSQL (Rails source database)
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/egghead_test

# MySQL (Coursebuilder target database)
NEW_DATABASE_URL=mysql://root:root@localhost:3307/coursebuilder_test

# SQLite (Mux video tracker)
SQLITE_PATH=./docker/sqlite/test.db

# Sanity Mock Server
SANITY_GRAPHQL_URL=http://localhost:4000/graphql
